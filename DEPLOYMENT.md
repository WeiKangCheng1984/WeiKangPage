# 部署指南：GitHub + Vercel → 未來 Cloudflare

## 📋 部署前檢查清單

### ✅ 1. 代碼準備

- [ ] 確認所有敏感資訊已從代碼中移除
- [ ] 檢查 `.gitignore` 是否正確配置
- [ ] 確認沒有硬編碼的 API keys 或密碼
- [ ] 測試本地建置：`npm run build`
- [ ] 確認沒有 TypeScript 錯誤

### ✅ 2. 環境變數準備

目前專案沒有使用環境變數，但未來可能需要：
- API keys
- 資料庫連接字串
- 第三方服務認證

**建議：** 現在可以先不設置，未來需要時再添加。

---

## 🚀 第一步：上傳到 GitHub

### 1.1 初始化 Git 倉庫（如果還沒有）

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Personal portfolio website"
```

### 1.2 創建 GitHub 倉庫

1. 前往 [GitHub](https://github.com) 創建新倉庫
2. **倉庫名稱建議：** `personal-portfolio` 或 `my-portfolio`
3. **不要**勾選「Initialize this repository with a README」（因為你已經有代碼）
4. 選擇 **Public** 或 **Private**（根據你的需求）

### 1.3 連接並推送

```bash
# 添加遠端倉庫（替換 YOUR_USERNAME 和 YOUR_REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 推送代碼
git branch -M main
git push -u origin main
```

### 1.4 GitHub 設置建議

**分支保護（可選）：**
- Settings → Branches → Add rule
- 保護 `main` 分支，要求 PR 審核

**README 更新：**
- 確保 `README.md` 包含專案說明
- 可以添加部署狀態徽章

---

## 🌐 第二步：部署到 Vercel

### 2.1 連接 GitHub 倉庫

1. 前往 [Vercel](https://vercel.com)
2. 使用 GitHub 帳號登入
3. 點擊 **Add New Project**
4. 選擇你的 GitHub 倉庫
5. 點擊 **Import**

### 2.2 Vercel 專案設置

**Framework Preset：**
- 自動偵測：Next.js ✅

**Root Directory：**
- 保持預設（`.`）✅

**Build Command：**
- 預設：`npm run build` ✅

**Output Directory：**
- 預設：`.next` ✅

**Install Command：**
- 預設：`npm install` ✅

### 2.3 環境變數（目前不需要）

目前專案沒有使用環境變數，可以跳過這一步。

**未來如果需要：**
- 在 Vercel 專案設置中添加環境變數
- 分別為 Production、Preview、Development 設置

### 2.4 部署

1. 點擊 **Deploy**
2. 等待建置完成（約 1-3 分鐘）
3. 部署成功後會獲得一個 Vercel 網址：`your-project.vercel.app`

### 2.5 自動部署設置

**已自動啟用：**
- ✅ 每次推送到 `main` 分支會自動部署
- ✅ 創建 Pull Request 會產生預覽部署
- ✅ 自動 HTTPS 憑證

---

## 🔒 第三步：域名設置（暫時方案）

### 3.1 使用 Vercel 提供的域名

**暫時使用：**
- `your-project.vercel.app`（Vercel 自動提供）

**優點：**
- 免費
- 自動 HTTPS
- 無需額外配置

### 3.2 添加自訂域名（可選，暫時）

如果你已經有域名，可以在 Vercel 中添加：

1. 專案設置 → Domains
2. 添加你的域名
3. 按照指示設置 DNS 記錄

**⚠️ 注意：** 如果未來要遷移到 Cloudflare，建議現在**不要**在 Vercel 綁定自訂域名，避免 DNS 衝突。

---

## ☁️ 第四步：為未來 Cloudflare 整合做準備

### 4.1 當前架構（暫時）

```
用戶 → Vercel (部署 + DNS) → 網站
```

### 4.2 未來架構（Cloudflare）

```
用戶 → Cloudflare (DNS + CDN + 安全) → Vercel (部署) → 網站
```

### 4.3 準備事項

#### ✅ 1. 保持 Vercel 作為部署平台

**原因：**
- Vercel 專為 Next.js 優化
- 自動 SSR/ISR
- Edge Functions 支援
- 預覽環境

**未來 Cloudflare 的角色：**
- DNS 管理
- CDN 加速
- 安全防護（WAF）
- 靜態資源（R2）

#### ✅ 2. 不要綁定自訂域名到 Vercel（如果計劃用 Cloudflare）

**建議：**
- 暫時使用 `your-project.vercel.app`
- 未來在 Cloudflare 設置 DNS 指向 Vercel

#### ✅ 3. 環境變數管理策略

**當前：** 在 Vercel 設置環境變數

**未來 Cloudflare：**
- 環境變數仍保留在 Vercel
- Cloudflare 只負責 DNS 和 CDN
- 不需要在 Cloudflare 重複設置

#### ✅ 4. 圖片和靜態資源

**當前：** 放在 `public/` 目錄，由 Vercel 提供

**未來 Cloudflare：**
- 可以遷移到 Cloudflare R2（大檔案）
- 使用 Cloudflare Images（自動優化）
- 小檔案仍可保留在 Vercel

---

## 📝 重要注意事項

### ⚠️ 1. 不要提交敏感資訊

**檢查這些文件：**
- `.env.local`（已在 `.gitignore` 中）
- `.env`（如果有，應該加入 `.gitignore`）
- API keys
- 資料庫連接字串

**確認 `.gitignore` 包含：**
```
.env*.local
.env
.vercel
node_modules
```

### ⚠️ 2. 建置錯誤處理

**如果建置失敗：**
1. 檢查 Vercel 建置日誌
2. 確認 `npm run build` 在本地可以成功
3. 檢查 Node.js 版本（Vercel 使用 18.x 或 20.x）

**設置 Node.js 版本（如果需要）：**
在 `package.json` 中添加：
```json
{
  "engines": {
    "node": ">=18.0.0"
  }
}
```

### ⚠️ 3. 圖片域名設置

**當前 `next.config.mjs`：**
```javascript
images: {
  domains: ["vercel.app"],
}
```

**未來加入 Cloudflare：**
```javascript
images: {
  domains: ["vercel.app", "your-domain.com", "imagedelivery.net"], // Cloudflare Images
}
```

### ⚠️ 4. 避免 DNS 衝突

**暫時方案：**
- 使用 Vercel 提供的域名
- 不要綁定自訂域名到 Vercel

**未來 Cloudflare：**
- 在 Cloudflare 設置 DNS
- 使用 CNAME 指向 Vercel：
  ```
  CNAME  your-domain.com  →  your-project.vercel.app
  CNAME  www.your-domain.com  →  your-project.vercel.app
  ```

---

## 🔄 未來遷移到 Cloudflare 的步驟（預覽）

### 步驟 1：DNS 遷移
1. 在 Cloudflare 添加你的域名
2. 將 DNS 從原註冊商轉移到 Cloudflare
3. 設置 CNAME 指向 Vercel

### 步驟 2：保持 Vercel 部署
- Vercel 繼續負責部署和建置
- Cloudflare 只負責 DNS 和 CDN

### 步驟 3：優化（可選）
- 啟用 Cloudflare CDN 快取
- 使用 Cloudflare R2 存儲大檔案
- 啟用 WAF（Web Application Firewall）

---

## 📊 部署後檢查

### ✅ 功能檢查

- [ ] 首頁正常載入
- [ ] 導航連結正常
- [ ] 遊戲頁面正常
- [ ] 部落格頁面正常
- [ ] 圖片正常顯示
- [ ] 動畫效果正常
- [ ] 響應式設計正常（手機、平板、桌面）

### ✅ 效能檢查

- [ ] 頁面載入速度
- [ ] 圖片優化
- [ ] 沒有 Console 錯誤

### ✅ SEO 檢查

- [ ] Meta 標籤設置
- [ ] 網站地圖（未來可以添加）
- [ ] robots.txt（未來可以添加）

---

## 🛠️ 常用命令

### 本地開發
```bash
npm run dev          # 啟動開發伺服器
npm run build        # 建置專案
npm run start        # 預覽建置結果
```

### Git 操作
```bash
git status           # 查看狀態
git add .            # 添加所有變更
git commit -m "..."  # 提交變更
git push             # 推送到 GitHub（會觸發 Vercel 自動部署）
```

### Vercel CLI（可選）
```bash
npm i -g vercel      # 安裝 Vercel CLI
vercel               # 部署到 Vercel
vercel --prod        # 部署到生產環境
```

---

## 📚 相關資源

- [Vercel 文檔](https://vercel.com/docs)
- [Next.js 部署文檔](https://nextjs.org/docs/deployment)
- [Cloudflare 文檔](https://developers.cloudflare.com/)
- [GitHub Actions](https://docs.github.com/en/actions)（未來可選）

---

## 🎯 總結

### 當前部署流程
1. ✅ 代碼推送到 GitHub
2. ✅ Vercel 自動部署
3. ✅ 使用 Vercel 提供的域名

### 未來 Cloudflare 整合
1. ⏳ 在 Cloudflare 設置 DNS
2. ⏳ CNAME 指向 Vercel
3. ⏳ 保持 Vercel 作為部署平台
4. ⏳ Cloudflare 負責 DNS、CDN、安全

### 關鍵原則
- **Vercel = 部署平台**（保持不變）
- **Cloudflare = DNS + CDN + 安全**（未來添加）
- **不要現在綁定自訂域名到 Vercel**（避免未來遷移麻煩）

---

**最後更新：2024-01-20**

